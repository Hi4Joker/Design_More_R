<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


  <item
      android:id="@+id/item_anton"
      android:title="Reveal Anton"
      android:visible="true"
      app:showAsAction="ifRoom|withText"/>
  <item
      android:id="@+id/item_normal"
      android:title="Normal FAB"
      android:visible="true"
      app:showAsAction="ifRoom|withText"/>
  <item
      android:id="@+id/item_footer_bar"
      android:title="Reveal footerBar"
      android:visible="true"
      app:showAsAction="ifRoom|withText"/>
  <item
      android:id="@+id/item_sheet"
      android:title="Reveal sheet"
      android:visible="true"
      app:showAsAction="ifRoom|withText"/>

</menu>